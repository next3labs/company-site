---
layout: layouts/base.njk
title: Software Development & Linux Server Rescue Singapore
description: Professional software development, Linux server rescue, and DevOps services in Singapore. We build custom software, fix crashed servers, and optimize systems. Emergency support available.
permalink: /
---

<section class="hero">
  <div class="container">
    <h1>AI Solutions for Singapore SMEs</h1>
    <p class="lead">Custom software development, AI agents, and managed IT services.</p>
    <p class="lead">Budget 2026 gives SMEs up to $50K/year in AI tax deductions. We'll help you claim them.</p>
    <a href="/ai-readiness/" class="btn btn-primary">Free AI Readiness Assessment</a>
  </div>
</section>

<section class="hero" style="background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%); padding: 4rem 0;">
  <div class="container" style="text-align: center;">
    <p style="color: #f59e0b; font-weight: 600; margin-bottom: 1rem;">⭐ Trusted by</p>
    <p style="color: white; font-size: 1.25rem;">Astons F&B · Security Association Singapore · Solution Details</p>
  </div>
</section>

<section class="services">
  <div class="container">
    <h2>What We Do</h2>
    <div class="services-grid">
      {% for service in services %}
      {% if service.link %}
      <a href="{{ service.link }}" class="service-card service-card-link">
        <div class="service-icon">{{ service.icon }}</div>
        <h3>{{ service.title }}</h3>
        <p>{{ service.description }}</p>
        {% if service.featured %}
        <span class="featured-badge">Featured Product</span>
        {% endif %}
      </a>
      {% else %}
      <div class="service-card">
        <div class="service-icon">{{ service.icon }}</div>
        <h3>{{ service.title }}</h3>
        <p>{{ service.description }}</p>
      </div>
      {% endif %}
      {% endfor %}
    </div>
    <div style="text-align: center; margin-top: 2rem;">
      <a href="/services/" class="btn btn-secondary">View All Services</a>
    </div>
  </div>
</section>

<section class="cta">
  <div class="container">
    <h2>Got a Technical Challenge?</h2>
    <p>Whether you need custom software built from scratch or a crashed server brought back to life, we've got you covered.</p>
    <p>Based in Singapore. Available for remote work worldwide.</p>
    <a href="/contact/" class="btn btn-primary">Get in Touch</a>
  </div>
</section>
